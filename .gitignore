__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
node_modules/
dist/
.cache/
store_data/
logs/
# 내 PC 가 쓰는 기록. 저장소의 learning/ 은 Actions 가 쓴다 — 나눠 두지 않으면 충돌한다.
learning-local/
# learning/ 은 일부러 추적한다 — 매일 학습이 뭘 배웠는지가 이 저장소의 내용이다.
# 모델(.pkl)은 store_data/ 아래라 안 올라간다. 무겁고, 설정만 있으면 다시 만든다.
# 판 하나하나의 기록. 하루에 수천 줄이 쌓여 공개 저장소에 올릴 크기가 아니다 —
# 다시 만들 수 있는 원자료이고, 요약(docs/STUDY.md)과 규칙(gate.json)만 올린다.
learning/study/verdicts.jsonl

# 타입스크립트 빌드 캐시. 커밋마다 내용이 흔들리고 다시 만들어진다.
web/tsconfig.tsbuildinfo
# 걸어 둔 알림·나간 알림·들고 있는 것. 이 PC 사람의 개인 기록이고, 얼마에 몇 주를
# 샀는지가 그대로 들어 있다. 공개 저장소에 올릴 것이 아니다.
alerts/
