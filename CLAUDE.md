# market-lens

차트를 읽고 분석·예측하는 도구. Python(FastAPI + pandas) 분석 + React/TS 프론트.
구조는 `README.md`, 여기는 **고칠 때 지켜야 하는 것**만 적는다.

## 표는 한 벌만

- **지표 목록은 `indicators/catalog.py` 하나다.** 지표를 더할 때는 카테고리 파일에
  `@indicator(IndicatorSpec(...))` 로 스펙 한 벌 + 순수 함수 하나를 쓰는 게 전부다.
  프론트의 지표 패널은 `GET /api/indicators` 가 준 카탈로그로 스스로 만들어진다 —
  **화면에 지표 이름을 다시 적지 말 것.** 두 벌이 되는 순간 어긋나기 시작한다.
- 계산 함수는 입력과 **같은 길이**의 DataFrame 을 돌려주고, 열 이름은 스펙의 출력 key 와
  정확히 같아야 한다. `compute` 가 거기서 걸어 준다.
- 평활 방식(EMA·RMA 시드)은 `indicators/_math.py` 한 곳에서만 정한다. 어디선 첫 값으로,
  어디선 SMA 로 시드하면 같은 화면 안의 20EMA 두 개가 다르게 그려진다.
- 색은 스펙에 이름(`accent`/`up`/`down`/`warn`/`neutral`)으로 적는다. hex 를 파이썬에
  적지 말 것 — 실제 색이 되는 곳은 `web/src/theme.css` 의 토큰 한 벌이다.

## 캔들 스키마

- 표준형은 `core/candle.py` 의 7열(`ts`/`open`/`high`/`low`/`close`/`volume`/`closed`)뿐이다.
  Binance 는 ms, Upbit 는 타임존 표기 없는 UTC 문자열, KIS 는 날짜·시각 두 필드로 준다.
  **그 차이는 프로바이더 안에서 끝낸다.** 엔진·API·화면은 시장을 모른다.
- `ts` 는 봉 **시작** 시각이고 UTC ms 다. 초로 바꾸는 곳은 `core/series.py` 한 곳뿐 —
  프론트에서 다시 나누지 말 것.
- 계약 위반은 `validate()` 가 문장으로 돌려준다. 프로바이더를 하나 더 만들면
  `tests/test_providers.py` 의 `assert_contract` 를 그대로 통과해야 한다.

## 봉이 언제 끝나나 — 장 마감을 안다

`ts + 봉길이 <= 지금` 은 암호화폐에는 맞지만 **주식에는 틀리다.** 코스피 일봉은
15:30 KST 에 끝나는데 그 식으로는 다음날 09:00 KST 에야 닫힌다. 그 사이 예측·추천은
하루 전 데이터로 돌아간다 — 실제로 29일 아침에 27일까지만 보고 예측하고 있었다.

- 판정은 `core/timeframe.py: bar_closed` **한 곳**. 프로바이더마다 다시 적지 말 것.
  `tests/test_session.py` 가 `+ step <= now` 를 프로바이더에서 찾으면 실패시킨다.
- 두 규칙을 **OR** 로 묶는다: 시각 계산 **또는** 그날 정규장 마감(`SESSION_END`).
  OR 인 게 중요하다 — 마감 표가 틀려도 시각 계산이 결국 닫으므로 **한 번 닫힌 봉이
  다시 열리는 일은 없다.** 그게 뒤집히면 그 위에서 잰 성적이 전부 거짓이 된다.
- 마감 뒤 `SETTLE_MS`(30분)를 더 기다린다. 종가·거래량이 확정돼 오기까지 몇 분 걸리고,
  그 사이 값을 확정봉으로 쓰면 마지막 봉만 조용히 틀린다.
- **일찍 닫는 게 늦게 닫는 것보다 나쁘다.** 모르는 시장은 시각 계산으로 두고, 장중
  봉(1m~4h)은 마감을 안 본다.
- 서머타임을 손으로 적지 말 것. `SESSION_END` 는 tz 이름(`America/New_York`)을 들고
  있고 변환이 그걸 처리한다. 오프셋을 박으면 반년마다 틀린다.
- 일봉 시각은 **현지 달력 날짜를 UTC 자정으로** 찍는 게 이 저장소의 약속이다
  (`toss._local_day_ms`, 야후도 결과적으로 같다). `bar_closed` 가 그 약속 위에서
  현지 날짜를 읽는다. KIS 만 아직 KST 자정(=UTC 전날 15:00)이라 격자를 벗어나 있고,
  그래서 마감 판정에는 `stck_bsop_date` 를 따로 쓴다 — 고칠 자리다.

## 리페인팅 — 제일 중요한 규칙

- **시그널 판정·백테스트·ML 라벨은 `closed_only()` 를 통과한 봉만 본다.** 미확정 봉을
  섞으면 장중에 떴다 사라지는 신호가 되고, 그 순간 백테스트 승률이 실전과 갈라진다.
  지표를 그리는 건 미확정 봉까지 해도 된다.
- 일목 선행스팬이 미래로 26봉 나가는 것은 정의상 정상이다. 후행스팬의 마지막 26봉이
  NaN 인 것도 정상이고, 그래서 시그널이 미래를 훔쳐보지 못한다. **그 NaN 을 메우지 말 것.**
- 백테스트는 **봉 i 의 판단으로 봉 i+1 의 시가에 체결한다.** 같은 봉 종가에 체결하면
  그 종가를 보고 판단한 셈이 된다.
- 스윙(`structure.find_swings`)은 오른쪽 n봉이 채워져야 확정한다. 이걸 당기면
  그 위에 그린 피보나치가 새로고침마다 자리를 옮긴다.
- 고쳤으면 `tests/test_signals.py` 의 `test_streaming_matches_batch` 와
  `test_signal_ignores_the_forming_bar` 를 반드시 돌릴 것. 여기가 깨지면
  지표가 틀린 것보다 나쁘다 — 숫자가 전부 거짓말이 된다.

## 프로바이더

- 메서드는 `history` / `stream` 둘뿐이다. 시장 분기(`if market == "kr"`)를 엔진·API·
  화면에 흘리지 말 것.
- **키가 없어도 앱은 뜬다.** 그 프로바이더만 `available=False` 로 목록에 남는다.
  하나 빠졌다고 전체가 안 뜨면 처음 여는 사람이 아무것도 볼 수 없다.
- 체결 틱을 봉으로 접는 규칙은 `base.CandleAggregator` 하나다. Binance 만 완성된 봉을
  주고 나머지 셋은 체결만 준다 — 프로바이더마다 따로 접으면 같은 1분봉이 거래소마다
  다르게 닫힌다.
- 미국주식은 히스토리(야후)와 실시간(Finnhub)의 출처가 다르다. **그 이음매는
  `composite.py` 한 곳에만 둔다.** Finnhub 무료 티어는 과거 캔들을 403 으로 막는다 —
  이건 설정 문제가 아니니 우회하려 하지 말 것.
- 히스토리 소스는 갈아끼워질 수 있다고 보고 짠다. Stooq 를 쓰다가 봇 차단에 막혀
  야후로 바꿨는데, 프로바이더 계약 밖으로 아무것도 새지 않아 파일 하나만 갈면 끝났다.
  야후도 공식 API 가 아니다 — 언젠가 같은 일이 난다.
- 토스증권도 같은 이유로 파일 하나(`toss.py`)로 들어왔다. 지표·유사구간·이벤트·학습·
  화면은 한 줄도 안 고쳤다. **새 증권사를 붙일 때 이 파일들을 건드리게 된다면 그건
  프로바이더 계약이 새고 있다는 뜻이다.**
- 한 프로바이더가 두 시장을 덮으면 **등록을 둘로 쪼갠다**(`toss_kr` / `toss_us`).
  `info.market` 이 사건 범위(`market:kr`)를 정하므로, 하나로 묶으면 국내 사건과
  미국 사건이 같은 이벤트 스터디에 섞인다.
- 거래소로 나가는 연결은 (프로바이더, 심볼, 타임프레임) 당 **하나**다(`api/ws.py` 의 Room).
  탭마다 새로 연결하면 거래소 한도에 먼저 걸린다.

## 화면

- **지표 계산은 서버에서만.** 프론트는 받은 시리즈를 그리기만 한다. 양쪽에서 계산하면
  반올림과 시드값 차이로 같은 화면 안의 값이 어긋난다.
- `optional` 출력은 "기본으로는 숨기는 보조선"이다. 그리면 볼린저의 %B 같은 게
  제멋대로 서브패널을 만든다.
- 판단에는 **근거 문장이 반드시 붙는다.** "매수 신호" 만 뜨는 화면은 아무것도 알려주지
  않는다. 숫자를 넣어 왜 그렇게 봤는지가 읽히게 쓸 것.
- UI 를 고쳤으면 `cd web; npm run shot` 하고 `screenshots/*.png` 를 Read 로 직접 볼 것.
  차트는 캔버스라 DOM 스냅샷으로는 아무것도 안 보인다. 특히
  `chart-ichimoku.png` 는 구름이 마지막 봉보다 앞까지 뻗는지를 보는 유일한 방법이다.

## 예측 — 유사구간과 이벤트

이 프로그램의 예측은 "지금과 비슷했던 과거를 찾아 그때 이후를 본다" 하나다.
모델이 미래를 만들어내는 게 아니라 과거를 꺼내 오는 것이므로, **미래를 훔쳐보지 않는 것**이
정확도보다 먼저다. 훔쳐보면 예측이 예쁘게 맞고, 그 예쁜 숫자를 믿고 돈을 잃는다.

미래를 막는 장치가 다섯 군데 있다. 하나라도 지우면 나머지가 다 무의미해진다:
1. `analog/matcher.py` — 후보는 결과(이후 horizon 봉)가 **이미 나온 자리**만.
2. 같은 파일 `_drop_overlaps` — 겹치는 창을 버린다. 안 버리면 좋은 자리 하나 주변의
   창 스무 개가 뽑혀 "사례 20건" 이 되는데, 실제로는 한 건이다.
3. `context/regime.py`·`events/detectors.py` — 백분위를 **rolling 으로** 잰다.
   전체 구간 분포로 자르면 나중 데이터가 옛날 판정을 바꾼다.
4. `indicators/structure.py: find_swings` — 오른쪽 n봉이 채워져야 스윙을 확정한다.
5. `events/study.py` — 추정창과 사건창 사이에 틈(gap)을 둔다.

- **사례 수와 거리를 반드시 같이 낸다.** 사례가 셋뿐인 확률 60%와 스물인 확률 60%는
  다른 말이다. `projection.diagnostics.reliable` 이 그 판정이고, 화면이 경고를 띄운다.
- 밴드는 그냥 믿지 않는다. 사례 하나를 빼고 만든 밴드가 그 사례를 덮었는지 세어
  (`coverage`) 목표에 못 미치면 넓힌다. 좁히지는 않는다 — 사례가 적을 때 우연히 높게
  나온 커버리지로 밴드를 조이면 위험하다.
- 상황 벡터(`context/features.py`)의 축은 **전부 무차원**이어야 한다. 가격·거래량을
  날것으로 넣으면 5만짜리 BTC 와 7만원짜리 주식이 비교 불가능해진다. 축 하나만 범위가
  크면 그 축이 거리를 독점한다 — `test_context_axes_stay_bounded` 가 잡는다.
- 캘린더 축(요일·시각·분기)은 **예측이 아니라 유사구간을 고르는 조건**으로만 쓴다.
  캘린더 효과는 발표 이후 사라진 사례가 많다(`research.library: calendar_effects`).

## 사건

- 표준형은 `events/schema.py` 하나. 출처가 여섯이라(사전·달력·차트탐지·GDELT·FRED·직접등록)
  형태를 하나로 묶지 않으면 이벤트 스터디가 출처별 분기로 뒤덮인다.
- **소스가 범위를 안 지킨다고 가정한다.** `collector.clip` 이 차트 구간 밖 사건을 버린다 —
  FRED 가 월별 시리즈에서 날짜 파라미터를 무시하고 1947년부터 전부 돌려준 적이 있고,
  그때 사건이 717건으로 부풀어 화면 숫자가 통째로 거짓말이 됐다.
- 같은 봉의 탐지는 하나로 접는다(`detectors._merge_same_bar`). 급락이 나면 거래량도
  터지고 변동성도 뛴다 — 셋을 따로 세면 이벤트 스터디가 같은 날을 세 번 센다.
- 신고점·신저점은 쿨다운을 둔다. 하락 추세에서는 몇 봉마다 신저점이라 목록이 도배된다.
- `seed_events.json` 에는 **날짜가 확실한 것만.** 최근 사건은 뉴스 소스나 직접 등록으로 받는다.
  FOMC 처럼 해마다 바뀌는 일정을 코드에 박으면 반드시 틀린다.
- 달력에서 계산되는 것(월말·분기말·만기)은 저장하지 않는다. 계산은 안 밀린다.

## 질문과 시나리오

- 자연어든 폼이든 결국 같은 `Scenario` 가 된다. LLM 이 바로 답을 내면 왜 그 답이
  나왔는지 검증할 방법이 없다 — **해석을 화면에 보여주고 사람이 고칠 수 있어야 한다.**
- `parser.parse_rules` 를 지우지 말 것. 키가 없을 때의 폴백이자 LLM 결과의 비교 기준이다.
- **조건을 풀었으면 반드시 알린다**(`scenario.notes`). 조용히 넘어가면 사용자는 조건이
  먹은 결과라고 믿는다. 그게 이 프로그램이 할 수 있는 제일 위험한 거짓말이다.
- Claude 호출은 질문 한 줄을 폼으로 옮기는 일이다. `effort: low` 로 충분하고,
  실패하면 규칙 결과로 조용히 내려간다.

## 근거 등록부

- 새 방법론을 넣을 때는 `research/library.py` 에 항목을 **먼저** 쓴다. 항목 없이 들어간
  방법론은 "어디선가 본 것" 이고, 반년 뒤에 검증할 수 없다.
- `limits` 를 비워 두면 등록이 거부된다(`registry.add`). 어떤 조건에서 통하지 않는지가
  실전에서 더 중요하다.
- `confidence` 에 `contested` 를 쓰는 걸 두려워하지 말 것. 기술적 분석의 예측력은 실제로
  논쟁적이고, 다 강하다고 적으면 등록부가 장식이 된다.

## 학습층

검색 기반은 "비슷한 과거가 이랬다"까지다. 학습층은 **그 요소들을 입력으로 넣고 실제로
무슨 일이 났는지를 배운다** — 사례를 답으로 쓰는 대신 "지금은 사례를 믿을 자리인가"까지.

- **목표값도 무차원이어야 한다.** 피처만 무차원으로 만들고 목표값을 절대 로그수익률로
  두면, 변동성 수준이 바뀌는 순간 트리가 학습 범위 밖으로 못 나가 무너진다.
  실측으로 skill −0.35 였다. 현재 ATR 로 나눈 뒤 예측을 다시 곱해 되돌린다.
- **기준선이 둘이다.** 단순(무조건부 분위수)과 변동성(k × 현재 ATR). 학습이 의미가
  있으려면 **변동성 기준선**을 넘어야 한다 — 변동성 뭉침만으로 이미 상당히 맞기 때문이다.
- **못 넘으면 모델을 쓰지 않는다.** `predict` 가 기준선을 그대로 쓰고 `source` 로 알린다.
  진 모델을 쓰면 더 나쁜 답이 나온다. 실측상 BTC 1시간봉·1일봉, ETH 4시간봉 모두 못 넘었다 —
  **이건 버그가 아니라 측정 결과다.** 종목·봉마다 다르므로 학습할 때마다 다시 잰다.
- 성적을 좋게 만들려고 기준선을 약하게 바꾸지 말 것. 그건 도구를 거짓말쟁이로 만드는
  제일 쉬운 방법이다.
- `analog_features` 는 봉마다 '그때 검색했으면 나왔을 것'을 만든다. 뒤쪽 데이터를 잘라도
  앞쪽 값이 같아야 하고, `test_analog_features_never_use_the_future` 가 그걸 지킨다.
- 봉마다 검색을 다시 돌리면 학습 표를 못 만든다. z-정규화한 창끼리는 거리가 상관계수
  하나로 줄어들어(‖z₁−z₂‖² = 2n(1−corr)) 행렬 곱 한 번이면 끝난다.
- 밴드는 컨포멀로 보정한다. 검증 구간 밖으로 벗어난 정도를 모아 목표 커버리지만큼 넓힌다 —
  실측 80% 밴드 적중률이 0.800 으로 맞는다. 이 수치가 어긋나면 보정이 깨진 것이다.
- 예정된 사건은 실제로 미리 알 수 있지만 피처에 쓰지 않는다. 어느 사건이 사전에 알려진
  것인지 일일이 따져야 하는데, 학습 표에서 미래가 새는 제일 흔한 경로가 거기다.

## 학습 기법은 재고 나서 넣는다

`scripts/sweep.py` 가 기법 실험 장치다. 모델 코드에 먼저 넣고 나중에 재면, 안 되는
기법이 그대로 남는다. **여기서 재고 이긴 것만 옮긴다.**

재 본 결과(2026-08, BTC·ETH·SOL·XRP·BNB·DOGE 1시간봉 / 그것들 + AAPL·MSFT·NVDA·^GSPC 일봉):

| 기법 | 결과 |
|---|---|
| 목표값을 ATR 단위로 | skill −0.35 → −0.11. **제일 큰 한 방** |
| 강한 정규화(작은 트리) | −0.35 → −0.06 |
| 여러 종목 풀링 | 표본 4천 → 1만7천 행 |
| 관심도 축(위키백과) | 1시간봉 −0.060 → −0.019 |
| **기준선과 섞기** | 음수 skill 을 양수로. **결정적** |
| 최근가중(반감기 180일) | **일관되게 해롭다.** 넣지 말 것 |
| 지평 늘리기 | 1시간봉은 24→72→168 다 안 됨 |
| **표본 10배 + 짧은 지평** | 시간봉을 뒤집은 것. 6종목·4천봉 −0.11 → **12종목·1.2만봉 +0.010** |
| 시장요인(시장수익률·상대강도·베타) | 총수익률에는 거의 중립 |
| 시각 조건부 변동성 | 암호화폐는 24시간이라 효과 없음. 주식에서는 다를 수 있다 |
| 미시구조(종가위치×거래량) | 소폭. 해롭지는 않아 남겨 둠 |
| **잔차(알파) 목표** | **+0.030.** 총수익률의 3배. 다만 아래를 볼 것 |

- **표본이 갈랐다.** "시간봉은 안 된다"가 아니라 "몇천 행으로는 안 된다"였다.
  같은 코드가 6종목 4천봉에서 −0.11, 12종목 1.2만봉에서 +0.010 이다.
  `routes.TRAIN_BARS` 가 봉 단위마다 필요한 깊이를 들고 있다 — 줄이지 말 것.
  **상한은 `TRAIN_MAX`(2만)지 `MAX_LIMIT`(5천)이 아니다.** 차트 한 번에 그릴 봉 수로
  학습을 막으면 시간봉이 5천봉에 잘린다. 실제로 그렇게 잘려 있었고, 스윕에서 +0.010
  이 나온 자리가 자동 학습에서는 +0.000 으로 보였다.
- **알파는 예측되는데 가격은 안 된다.** 시장 공통요인을 뺀 잔차는 skill +0.030 으로
  총수익률(+0.010)의 세 배다. 그런데 가격 밴드를 만들려면 그 시장 성분을 도로
  더해야 하고, 그건 예측 불가라 합성하면 +0.000 으로 돌아온다.
  **그러니 "알파를 잘 맞힌다"를 "가격을 잘 맞힌다"로 옮겨 적지 말 것.**
- 되는 자리는 `routes.LEARNABLE` 에 실측으로 박아 두고 화면이 미리 알린다 —
  15분봉 4~24봉, 시간봉 3~12봉, 일봉 5~20봉. **일봉보다 세세한 쪽도 된다.**
- **skill 이 양수인 것과 방향을 맞히는 것은 다르다.** 이득의 대부분은 밴드 폭·모양에서
  나온다. as-of 에서 암호화폐 시간봉 방향은 45% 였는데 같은 구간의 skill 은 양수다.
- 섞는 비중은 **앞 폴드에서 고른 값을 다음 폴드에 쓴다.** 검증 구간에서 고르면
  자기 답을 보고 만든 성적이라 못 믿는다(그렇게 재면 +0.009, 정직하게 재면 +0.005).
- 여러 종목을 모으면 **폴드를 시각으로 접어야 한다**(`time_folds`). 행 번호로 접으면
  같은 날의 다른 종목이 학습과 검증에 나뉘어 들어간다.
- 이긴 폭이 작다(+0.005 안팎). 화면이 "방향을 맞힌다"로 읽히지 않게 쓸 것.

## as-of 검증 — "그날 서서 예측하고 나중에 맞춰 보기"

`scripts/asof.py`. 학습 성적표(pinball skill)는 "분위수를 잘 맞혔나"지, "실제로 얼마나
맞았나"가 아니다. 사람이 알고 싶은 건 뒤쪽이다.

- origin 마다 **그 이전만 잘라** 예측하고, 지평이 지난 뒤 실제와 맞춘다.
  학습은 8개 origin 마다 다시 한다(매번은 너무 비싸고, 한 번만 하면 미래를 본다).
- 자르는 곳이 넷이다 — 시세·사건·유사구간 후보·학습 표. `tests/test_asof.py` 가
  "origin 뒤 데이터를 3배로 흔들어도 그 시점 예측이 안 변한다"로 지킨다.
- 재 본 결과(일봉 10봉 지평, origin 40개/종목):

| 종목 | 방향적중 | 경로상관 | 오차% (학습 / 기준선) |
|---|---|---|---|
| ^GSPC | **80.0%** | +0.384 | **1.40% / 1.71%** |
| AAPL | **72.2%** | +0.311 | 2.92% / 2.79% |
| BTCUSDT | 54.1% | +0.016 | 4.64% / 4.97% |
| ETHUSDT | 51.4% | +0.016 | 6.77% / 6.75% |

- **주식·지수는 맞고 암호화폐는 안 맞는다.** 이 차이가 이 도구에서 제일 큰 차이다.
- 표본이 종목당 35~40개다. 80% 는 95% 신뢰구간으로 대략 63~92% — 방향은 있지만
  소수점까지 믿을 숫자가 아니다.
- **기준선의 밴드 적중률이 90%를 넘는다**(목표 80%). 기준선 밴드가 너무 넓다는 뜻이고,
  그래서 "기준선보다 밴드가 좁으면서 적중률이 비슷하면" 이긴 것이다.
- as-of 도 **동료 종목까지 모아 학습한다**(`--peers`, 기본 12). 한 종목만 쓰면 표본이
  몇천 행이라 실력을 실제보다 낮게 재게 된다 — 스윕이 잰 게 정확히 그거였다(같은 코드가
  6종목 4천봉 −0.11, 12종목 1.2만봉 +0.010). **예측은 그대로 그 종목만 본다.**
  동료는 학습 표를 키울 뿐이고, `cut` 이 동료도 같이 자른다 — 하나라도 안 자르면
  그 종목의 미래가 학습 표로 새서 이 파일이 재는 것이 아무 뜻도 없어진다.
  **위 표는 단일 종목 시절(`--peers 0`)의 숫자다.** 풀링으로 다시 재면 갱신할 것.

## 매일 스스로 학습 — 챔피언과 도전자

`scripts/daily.py`. 매일 한 번, GitHub Actions(05:00 KST)와 내 PC(06:00, 작업 스케줄러
`market-lens-daily`) 양쪽에서 돈다.

```
챔피언을 새 데이터로 재학습 → 설정을 하나만 흔든 도전자도 학습
→ 같은 퍼징 워크포워드 blendSkill 로 재고 → 마진 이상 이길 때만 갈아끼운다
```

- **챔피언은 `model_name()` 자리에 그대로 덮어쓴다.** 그래야 그날부터 화면이 새 모델을
  쓴다. 도전자는 `-trial` 이름으로 옆에서 굽고, 이겼을 때만 파일을 옮긴다 —
  같은 이름으로 구우면 진 도전자가 챔피언을 덮어써 버린다.
- **손잡이는 한 번에 하나만 흔든다**(`window`·`neighbours`·`folds`·`peer_count`·
  `horizon`). 둘을 같이 바꾸면 뭐가 이겼는지 알 수 없다.
- **승격에 마진(+0.002)이 있다.** 0 이면 잡음으로 매일 모델이 바뀐다. 자동화 중에
  제일 나쁜 종류다 — 아무도 안 보는 사이 조용히 나빠지고 로그는 매일 "승격"이라 적는다.
- **오늘 돌릴 대상은 `최근 skill + 오래됨 보너스`로 고른다.** 되는 자리에 시간이 몰리되
  지는 자리도 버리지 않는다. 시장이 변하면 되는 자리가 뒤집히기 때문이다.
- **시험 횟수를 기록에 남긴다.** 같은 데이터로 수백 번 시험하면 그중 하나는 반드시
  이긴다. 이걸 숨기면 자동 탐색은 그냥 과최적화 기계가 된다.
- **승격 판정은 워크포워드로만.** as-of(`scripts/asof.py`)는 따로 재서 기록만 하고
  승격에 안 쓴다. 승격 기준에 넣는 순간 as-of 도 학습 구간이 된다.
- 남는 것: `learning/champions.json`(현재 최선) · `learning/log.jsonl`(매 실행 한 줄) ·
  `docs/LEARNING.md`(사람이 읽는 요약). 모델 `.pkl` 은 커밋하지 않는다.
- 서버는 학습을 돌리지 않는다. `api/learning.py` 가 기록을 **읽기만** 한다.
- **토스(국내주식)는 Actions 에서 못 돈다**(IP 허용 목록). PC 쪽만 국내주식을 학습한다.

## 아침 매수 추천

`scripts/recommend.py` 가 07:30 KST 에 한 번 뽑아 **날짜별 파일로 얼린다.**
화면은 그걸 읽기만 한다(`api/recommend.py`). "그날은 안 바뀐다"를 캐시가 아니라
**데이터의 성질**로 만든 것이다 — 누를 때마다 답이 달라지면 그건 추천이 아니다.
그래서 **POST 를 만들지 않는다.** 요청 시점에 계산하는 게 없다.

- **날짜는 KST 다.** 07:30 KST 는 **22:30 UTC(전날)** 라, UTC 로 파일 이름을 지으면
  "오늘의 추천"에 어제 날짜가 뜬다. `pd.Timestamp.now(tz="Asia/Seoul")`.
- **날짜마다 모델을 따로 굽는다**(`recommend-<provider>-1d-<day>`). 지평 3 하나로
  1·2·3일을 다 뽑으면, 그 모델이 3일에서 기준선을 못 넘는 순간(실제로 그랬다 —
  skill 0.0013 < 0.002) `predict` 가 기준선만 쓰면서 1일·2일에 있던 우위
  (0.0055 · 0.0031)까지 같이 버린다.
- **모델을 하나도 안 쓴 날은 그 사실이 화면까지 간다**(`degenerate`). 그때 기대값은
  그 종목 ATR 의 함수라 부호가 전부 같아지고 **순위가 사실상 '변동성 순서'로 무너진다.**
  판정은 `predict` 가 돌려준 `source == "volatility-baseline"` 로 한다 —
  `report["weights"]` 를 보면 안 된다. `useModel` 이 꺼지면 그 값과 무관하게 0 이 된다.
- **`study-*` 모델을 재사용하지 말 것.** `scripts/study.py` 가 그 이름들을 과거 origin
  에서 잘라 밤새 재학습한다. 아침에 쓰면 며칠 전까지만 본 모델로 추천하게 된다.
- **기대값은 `expectedMovePct` 가 아니라 밴드에서 읽는다.** 기권 규칙이 켜지면 그 필드가
  비워지는데(`model.py`) 밴드는 일부러 남겨 두므로, 밴드에서 읽으면 그때도 순위가 난다.
- 종목마다 학습하지 않는다. `predict(df, name, …)` 는 심볼을 안 보고 목표값이 무차원이라
  **한 모델로 그 시장 전부**를 예측한다. 종목마다 구우면 쉰 번을 구워야 한다.
- **화면이 시장을 고르게 하지 않는다.** 추천 탭은 국내주식·해외주식·코인 **세 묶음**을
  한 번에 낸다(`api/recommend.py: GROUPS` → `GET /api/recommend/groups`). 예전에는
  차트가 선 시장 하나만 보여줬는데, 차트 기본이 BTCUSDT 라 열면 늘 코인만 나왔다 —
  나머지 둘은 시장을 바꿔야 볼 수 있었고, 그러려면 그런 게 있다는 걸 먼저 알아야 했다.
- 한 묶음에 프로바이더가 여럿인 건 같은 종목을 여러 곳에서 받아서다. **앞에 적은
  것이 이긴다** — 토스가 앞인 건 한글 이름이 붙어서고, 바이낸스가 앞인 건 모델이
  거기서 학습되고 원화 시세만 업비트에서 붙기 때문이다.
- **빠진 묶음도 자리를 지킨다.** 국내주식은 토스가 IP 허용목록을 타서 Actions 에서
  못 돈다. 없는 것을 안 보여주면 "국내주식은 살 게 없다"로 읽힌다 — 사유를 적는다.
- **묶음끼리 견주지 말 것.** 순위는 그 묶음의 후보 안에서만 매겨지고 기준선도 그
  묶음의 평균이다. 코인 +2% 와 국내주식 +0.5% 는 변동성이 자릿수로 달라 같은 자로
  잰 값이 아니다. 성적표도 묶음마다 따로 센다.
- `BUY`·`AVOID` 는 **둘 다 3** 이다. 셋을 나란히 놓으므로 양쪽이 같은 크기여야
  한 줄로 읽힌다. `onPick` 은 **시장도 같이 넘긴다** — 코인 목록에서 삼성전자를
  눌렀을 때 바이낸스에 없는 심볼을 묻게 된다.
- **지수를 표에서 빼지 말 것.** `universe.buyable()` 이 매수 후보에서만 거른다.
  지수는 학습 동료로 남아야 한다 — "시장이 통째로 밀린 날"과 "이 종목만 밀린 날"을
  가르는 축이다. `NOT_BUYABLE` 은 이름으로 적는다(접두사 `^` 로 거르면 프로바이더
  일곱 곳의 표기 규칙에 대한 추측이 된다).
- **`move_atr` 로 재정렬하지 말 것.** 확신도 **라벨**로만 쓴다. 이 저장소는 이미 한 번
  빠졌다 — `move_atr < 0.038 이면 기권` 이 최종 구간에서 크게 이겼는데 알고 보니
  음수 예측을 통째로 버리는 상승장 편향이었다(`docs/STUDY.md`).
- **기준선은 후보 전체 평균이다**(추천 자신도 포함). 0 과 견주면 고르는 실력이 아니라
  시장을 잰다 — 시장이 다 오른 날 추천도 올랐다는 건 아무 말도 아니다.
  추천을 뺀 나머지와 견주는 것도 안 된다. 같은 표본을 두 조각으로 나눠 비교하는 셈이다.
- **채점은 `lastTs` 에 앵커한다, 파일 날짜가 아니라.** 그게 예측이 딛고 선 마지막
  확정봉이고 예측의 유일한 원점이다. 날짜로 맞추면 공휴일 하나에 조용히 밀린다.
- **채점 중복을 막는다**(`(날짜, 시장, 일수)`). 재실행이 정상인 스크립트라 안 막으면
  표본이 두 배가 되고 모든 비율이 조용히 좋아진다.
- 아직 결과가 안 나온 판은 **실패가 아니라 기다림**이다. 건너뛰고 내일 다시 잰다.
- **채점이 먼저, 뽑기가 나중.** 뽑기가 터진 날에도 화면의 '지난 성적'은 최신이어야 한다.
- 읽을 때 **두 폴더를 프로바이더 단위로 합친다**(로컬 우선). Actions 는 토스를 못 돌아
  국내주식이 PC 쪽에만 있다. `api/learning.py` 의 챔피언은 일부러 하나만 읽는다 —
  거긴 모델 파일과 짝이라 반쪽만 가져오면 어긋난다. **둘을 통일하지 말 것.**
- 시장마다 **자기 날짜를 들고 온다.** PC 가 일주일 꺼져 있었으면 국내주식은 8/22 것이
  오고, 화면이 그 날짜를 찍는다. 사라지지도, 오늘 것인 척하지도 않는다.
- 화면의 실측 숫자(방향 55.0% · 밴드 82.2%)를 **TSX 에 박지 말 것.**
  `api/recommend.py: measured()` 가 `study/state.json` 에서 읽어 넘긴다 — 그래야
  학습 실행과 절대 어긋나지 않는다.
- **`.cmd` 는 CRLF 로, 그리고 ASCII 로만 쓴다.** 이 파일들은 1980년대 파서가 애매한
  코드페이지로 읽는다. LF 면 줄을 못 끊고, 한글이 들어가면 cmd.exe 가 OEM(949)으로
  읽어 통째로 깨진 뒤 **깨진 조각을 명령으로 실행하려 든다.** 두 번 났다:

      '07:30' is not recognized as an internal or external command
      '???글을' is not recognized as an internal or external command

  `chcp 65001` 로 덮어 봤지만 그래도 샜다. 파서를 이기려 하지 말고 헷갈릴 거리를
  없앤다 — **`.cmd` 는 실행 순서만 담고, 이유는 여기 CLAUDE.md 에 적는다.**
  `test_scripts.py` 가 CRLF·ASCII 둘 다 막는다.
  (Windows 의 Python `write_text` 는 자동으로 CRLF 로 쓴다. bash heredoc 과 Write
  도구는 LF 라 걸린다 — 쓴 뒤 `read_text` → `write_text` 로 한 번 되돌리면 된다.)
- **`.cmd` 가 부르는 파이썬은 `-u` 로 돌리고, 스크립트는 stdout 을 UTF-8 로 맞춘다.**
  버퍼가 켜져 있으면 도중에 끊긴 실행이 로그를 **한 줄도** 안 남긴다(06:00 학습이
  3분 돌다 재부팅에 끊겼는데 그 3분이 통째로 없었다). 그리고 리다이렉트된 stdout 은
  기본이 cp949 라, 그 밖의 글자(`—`) 하나에 `UnicodeEncodeError` 로 죽는데 `@echo off`
  라 그 사실이 조용히 묻힌다. `daily.log` 가 여태 cp949 로 쌓이고 있었다.
- **시작·끝·종료코드를 로그에 적고, 다음 실행이 지난 실행을 확인한다**(`runcheck.py`).
  스케줄러의 진단 기록(`TaskScheduler/Operational`)은 **관리자 권한이 있어야 켜지고**
  기본이 꺼져 있다. 그래서 로그 자체가 답하게 만든다 — **마지막 START 뒤에 END 가
  없으면 그 실행은 죽은 것이다.** `runcheck` 는 자기 START 를 찍기 **전에** 돌아야 한다.
- **작업이 부르는 것은 `.cmd` 가 아니라 `pythonw.exe scripts/runjob.py <그 .cmd>` 다.**
  한동안 "스케줄러가 `recommend.py` 를 1초 안에 죽인다"(`0xC000013A`)로 남아 있던
  것의 정체는 **콘솔 창이었다.** 프로세스에 콘솔 컨트롤 핸들러를 달아 찍어 보니
  오는 것이 `CTRL_C`(스케줄러의 중지)가 아니라 **`CTRL_CLOSE`** — 누가 죽인 게
  아니라 붙어 있던 **창이 사라진** 것이라, 로그에도 이벤트에도 아무것도 안 남는다.
  25초 프로브로 재 본 것: `cmd.exe /c`·`python.exe` 는 1~6초에 죽고,
  `pythonw.exe`(콘솔 없음)와 `pythonw` → `cmd.exe`(`CREATE_NO_WINDOW`)는 끝까지 돈다.
  **닫힐 창이 없으면 닫히지도 않는다** — `chcp` 로 인코딩을 이기려다 실패했을 때와
  같은 답이다. 순서는 그대로 `.cmd` 안에 있고 `runjob.py` 는 창만 없앤다.
  (기계 쪽으로 고치는 길도 있다 — 기본 터미널을 Windows 터미널에서 콘솔 호스트로
  되돌리는 것. 저장소 밖의 설정이라 여기서는 안 건드린다.)
  ⚠ **`runjob.py` 에서 `print` 를 쓰지 말 것.** `pythonw` 는 stdout 이 `None` 이라
  한 줄에 죽고 그 사실도 안 남는다. `test_scripts.py` 가 막는다.
- 07:30 은 **06:00 학습과 따로 등록한다**(`market-lens-recommend`). 거기 붙이면 실제
  실행이 08시 언저리에서 매일 흔들린다. 겹치지 않게 `daily.cmd` 의 study 예산을
  1시간으로 줄여 뒀다.
- 고쳤으면 `.venv/Scripts/python -m pytest server/tests/test_recommend.py -q` 와
  `.venv/Scripts/python scripts/recommend.py --dry-run --provider binance`.

## 추천을 되돌려 채점한다 — 백필

`scripts/backfill.py`. 화면의 "이 추천이 과거에 맞았나" 는 실전 채점이 하루 한 줄씩
쌓여야 답이 나온다 — 한 달을 기다려야 한 달치다. 그래서 같은 추천을 **과거 아침에
세워** 뽑고, 지평이 지난 뒤 실제와 맞춰 그 표를 미리 채운다.

- **뽑기 함수를 따로 만들지 않는다.** `recommend.pick()` 은 넘겨받은 시세로 그
  자리에서 굽기 때문에, 시세를 origin 까지 잘라 넘기면 그대로 as-of 가 된다.
  채점도 `score_one` 그대로 쓰고 `closes` 로 시세 출처만 바꾼다. **표가 두 벌이
  되는 순간 "되돌려 본 성적"과 "실전 성적"이 다른 자로 잰 값이 된다.**
- 자르는 곳은 `cut()` 한 곳이고 갈래가 넷이다 — 시세·사건·관심도, 그리고 잘린
  시세에서 다시 만들어지는 학습 표. 관심도는 **ts 가 아니라 남은 확정봉 수**로
  자른다(그 표에는 ts 열이 없어서, ts 로 자르려 들면 조용히 안 잘린다).
- **8 origin 마다 다시 굽는다.** (시장 × 지평 3 × origin)이라 60일치가 180번이고
  몇 시간이다. 매번은 너무 비싸고 한 번만 하면 미래를 본다 — `scripts/asof.py` 가
  같은 벽에서 쓴 답이다.
- **모델 이름은 `backfill-*` 다.** `recommend-*` 를 과거 origin 에서 잘라 구운 것으로
  덮으면 며칠 전까지만 본 모델로 아침 추천을 하게 된다(`study-*` 재사용 금지와 같은 이유).
- **원화 시세를 붙이지 않는다**(`krw=False`). 그건 '지금' 값이라 과거 origin 에
  붙이면 거짓말이 된다.
- **파일이 다르다** — `scores.jsonl`(실전) · `backfill.jsonl`(되돌려 본 것). 섞으면
  못 믿을 숫자가 된다: 실전은 그날 한 번뿐이라 되돌릴 수 없고, 백필은 origin 을 고를
  수 있고 몇 번이든 다시 돌릴 수 있다. 화면도 칸을 나눠 어느 쪽인지 밝힌다.
- **줄마다 모델 이름과 설정을 박는다**(`config`). 손잡이는 `recommend.WINDOW`·`FOLDS`
  한 곳에서만 정하고, 바뀌면 `--restale` 이 옛 설정 줄을 버리고 그 자리만 다시 돌린다.
  옛 모델 성적과 새 모델 성적이 한 숫자에 섞이면 그게 정확히 못 믿을 숫자다.
- **뒤 20% 는 `holdout` 이다.** 추천 규칙(후보 수·기권 문턱·지평)은 앞 구간에서만
  만지고, 성적은 holdout 에서 읽는다. 튜닝에 쓴 구간의 성적은 자기 답을 보고 만든
  값이다 — `study.py` 의 최종 구간과 같은 이치다.
- **승격에 쓰지 않는다.** 모델 승격은 워크포워드(`daily.py`)로만 한다. as-of 를
  승격 기준에 넣는 순간 그것도 학습 구간이 되어 외부 표본이 아니게 된다.
  `test_backfill.py` 가 `daily.py` 에 `backfill` 이라는 낱말이 없는지로 잡는다.
- origin 마다 저장한다. 한 시장이 30분~1시간짜리라 끝에 한 번 저장하면 도중에
  끊길 때 통째로 사라진다.
- 고쳤으면 `.venv/Scripts/python -m pytest server/tests/test_backfill.py -q` 와
  `.venv/Scripts/python scripts/backfill.py --provider binance --origins 3 --every 3 --dry-run`.

재 본 결과(2026-08-30, 다섯 시장 × origin 60개, 자세한 것은 `docs/RECOMMEND.md`):

- **holdout 열다섯 칸 중 열둘이 음수다.** 고르는 실력이 안 보인다. 놀랄 일은 아니다 —
  횡단면 방향 스프레드는 하루 +0.03%p·사흘 +0.65%p 라 12판으로 잡힐 크기가 아니고,
  같은 구간 시장 움직임이 자릿수로 크다. **"나쁘다"가 아니라 "이 표본으로는 아무 말도
  못 한다"** 에 가깝지만, **좋다는 증거도 없다.**
- **국내주식이 처음엔 혼자 양수였는데(+0.57~+1.27%p), origin 을 60→200 으로 늘리자
  세 칸이 전부 음수로 뒤집혔다**(−0.25/−0.57/−0.90%p). 같은 규칙·같은 종목이고
  바뀐 건 어느 날들이 holdout 에 들어가느냐뿐이다. 게다가 튜닝 구간과 holdout 이
  **매번 서로 반대 부호**다 — 실력이 있으면 같은 쪽을 가리켜야 한다.
  **한 번 재고 부호를 믿지 말 것.** origin 을 늘려 보고, 두 구간이 같은 쪽인지 보고,
  `--test` 를 돌린다.
- **미국주식은 토스와 야후가 거의 같은 답을 냈다**(−0.15/−0.31/−0.43 대
  −0.20/−0.36/−0.51). 같은 종목을 다른 데서 받아 온 것이라, 측정이 데이터 출처의
  우연이 아니라는 확인이다.
- **밴드가 코인 최근 구간에서만 무너졌다**(튜닝 84~87% → holdout 52~72%, 지평이
  길수록 심함). 주식은 반대로 올랐다. 컨포멀 보정한 구간보다 뒤가 시끄러우면 밴드가
  좁게 남는다 — **여기가 진짜 고칠 자리다.** 다만 12판이라 52.5% 의 95% 구간이
  27~77% 다. **국내주식이 97.5% 라고 좁히지 말 것.**
- **잡음과 가르는 검정이 붙어 있다**(`--test`). 차이 계열을 **덩어리째** 다시 뽑아
  p 와 95% 구간을 낸다. 덩어리 길이는 `overfit.pick_block`(Politis & White)이 정한다 —
  손으로 고르면 p 가 그 임의의 숫자 위에 서게 된다. **칸을 몇 개 봤는지도 같이 낸다:**
  칸 30개면 귀무에서도 제일 작은 p 가 대략 0.032 근처에 온다. 실제 최소가 0.044 라
  **발견이 아니다.** 30칸 중 95% 구간이 0 을 벗어난 것은 하나도 없다.
- **이 표를 보고 규칙을 고치고 다시 재서 좋아진 숫자를 쓰면 안 된다.** 그러면
  holdout 이 튜닝 구간이 된다. 규칙을 만졌으면 origin 을 뒤로 밀어 새 holdout 을 만든다.

## 픽스처는 생산 코드가 만드는 것만 흉내낸다

`score_one` 이 `body["buy"]` 를 읽고 있었는데 얼린 파일에는 그런 키가 없었다
(추천 목록은 지평마다 다르니 `byDay[일수]` 안에만 있다). 실전 채점이 `KeyError` 로
죽는 코드였고, **채점이 ①단계라 뽑기까지 같이 못 돌 뻔했다.**

그런데도 몇 달을 살아남았다 — `_frozen` 픽스처가 `pick()` 이 만드는
모양이 아니라 **자기가 지어낸 모양**(`"buy": [{"symbol": …}]`)을 만들어 넣고 있었다.
지어낸 모양을 검사하는 테스트는 통과할수록 위험하다. 얼린 파일·나간 응답처럼
**다른 코드가 만드는 것을 받는 함수는, 그것을 만드는 코드의 출력으로 시험한다.**


## 종목 추천 — 재고 나서 줄 세운다

`screen/` + `scripts/screen.py`. "오늘 관심있게 볼 종목"을 횡단면으로 줄 세운다.

- **팩터를 새로 만들지 않는다.** 학습 표(`forecast/ml/dataset.py`)의 축에서 고르기만
  한다. 거기에 인과성 보증(`test_asof.py`)이 붙어 있고, 여기서 축을 다시 만들면
  그 보증 밖으로 나간다. 캘린더 축은 뺀다 — 같은 시각이면 전 종목이 같은 값이라
  순위가 안 갈린다.
- **부호를 미리 정하지 말 것.** "RSI 가 낮으면 산다" 를 코드에 적지 않는다. 랭크 IC 를
  재고, **폴드마다 부호가 일관된 축만** 쓴다(`ic.MIN_FOLDS` 3개 이상). 평균만 보면
  한 시기에만 통한 축이 살아남는다.
- **무게는 안 정한다.** 고른 뒤에는 동일 가중. IC 크기에 비례시키면 그 IC 를 잰
  구간에 무게를 맞춘 셈이다. 부호만 쓴다.
- **원값이 아니라 평소대비(`__rel`) 로 점수를 만든다**(`rank.FAMILY`). 원값 축
  (변동성·베타·밴드폭)은 IC 가 3~10배 크게 나오지만 그건 "DOGE 는 원래 BTC 보다
  많이 움직인다"는 **고정 순위**다. 늘 맞지만 매일 같은 답을 내므로 "오늘 뭘 볼까"에는
  쓸모가 없다. `scripts/screen.py` 는 전부/원값만/평소대비만 셋을 다 재서 남긴다 —
  나눠 보지 않으면 그냥 변동성 순위를 추천이라고 부르게 된다.
- **변동과 방향을 섞지 말 것.** `move`(얼마나 움직일까, |수익률| 기준)와
  `direction`(어느 쪽일까)은 강도가 자릿수로 다르다. 하나로 합치면 "크게 움직인다"가
  "오른다"로 읽힌다. 목록의 정렬은 `move` 고, 화면 문구가 그걸 못 박는다.
- **안 잰 지평은 순위를 안 만든다.** `learning/factors.json` 에 없는 지평은 빈 답을
  낸다. 그럴듯한 목록보다 빈 화면이 낫다.
- 후보 표는 `screen/universe.py` **한 벌**이고 `routes.PEERS` 가 그걸 그대로 가리킨다.
  두 벌이면 "재 본 목록"과 "추천하는 목록"이 갈라진다. 시장마다 최소 열 종목 —
  다섯에서 셋을 고르는 건 순위가 아니라 나열이다.
- 순위는 **확정봉**으로만 매긴다. 진행 중인 봉을 쓰면 같은 날 오전과 오후의 추천이
  달라지고, 그건 추천이 아니라 시세 중계다.
- 고쳤으면 `.venv/Scripts/python -m pytest server/tests/test_screen.py -q` 와
  `.venv/Scripts/python scripts/screen.py --provider binance --horizons 1 3 --dry-run`.

## 못 맞히는 자리에서는 말을 안 한다 — 자율 학습

`scripts/study.py` 가 과거 시점에 서서 예측하고(그 시점 이후는 안 본다), 지평이 지난 뒤
실제와 맞춘 다음, **맞은 판과 틀린 판을 조건별로 갈라** 본다. 갈라진 조건은 "이 조건이면
말을 안 한다"는 기권 규칙이 되고, 규칙은 **찾은 구간이 아니라 뒤 구간**에서 확인한다.

- 판 하나가 `learning/study/verdicts.jsonl` 한 줄이다 — 예측·실제·적중·**그때의 조건**.
  조건은 `dataset.build` 의 축을 그대로 쓴다. 여기서 축을 새로 만들면
  `tests/test_asof.py` 의 인과성 보증 밖으로 나간다.
- 판을 시간순으로 **탐색 60% / 확인 20% / 최종 20%** 로 나눈다. 규칙은 탐색에서 찾고,
  확인에서 거르고, **최종 구간은 열 라운드에 한 번만** 열어 본다. 볼 때마다 그 숫자가
  닳으므로 **본 횟수를 기록에 남긴다**(`holdoutLooks`).
- **최종 구간에서 잰 그 규칙만 내보낸다**(`learning/study/gate.json`). 확인 구간 1위는
  라운드마다 바뀌므로, 그걸 내보내면서 성적은 다른 규칙 것을 붙이게 된다. 실제로 그랬다.
- 규칙을 손으로 적지 말 것. `forecast/gate.py` 에는 **읽고 적용하는 코드만** 있다.
  임계값을 코드에 박는 순간 그건 측정이 아니라 믿음이고, `tests/test_gate.py` 가 막는다.
- **모르는 조건으로 입을 다물지 않는다.** 조건이 표에 없으면 기권하지 않는다.
- 기권해도 **밴드는 그대로 낸다.** 방향과 달리 밴드는 실제로 잘 맞는다(적중 78~80%).
- 학습은 **동료 종목까지 모아서** 한다. 한 종목만 쓰면 표본이 모자라 실력을 실제보다
  낮게 재게 된다 — 이 저장소가 이미 겪은 함정이다.
- 고쳤으면 `.venv/Scripts/python scripts/study.py --hours 0.1 --origins 8` 로 한 바퀴,
  그리고 `.venv/Scripts/python -m pytest server/tests/test_gate.py -q`.

## 방향을 골라내려는 시도는 세 번 졌다
- **방향 적중 55.0% 를 조건으로 골라내는 방법은 지금까지 전부 실패했다.**
  조건 하나 + 문턱 하나(`study.py`, 8시간): +0.55%p, p=0.38.
  축 36개 + 트리 모델(`metalabel.py`, 메타 라벨링): +0.94%p, p=0.365.
  자유도를 크게 늘려도 같은 자리에 떨어진다.
- 새로 시도하기 전에 **`docs/STUDY.md` 부터 읽을 것.** "모델을 더 얹으면 되지 않을까"
  는 이미 해 본 논거다. 새 근거(새 축, 더 많은 판, 다른 시장)가 있어야 열린다.
- 무엇을 하든 **`scripts/metalabel.py` 와 같은 귀무 검정을 통과시킨다** — 결과를
  덩어리째 섞어 200번 다시 돌리고 p 를 본다. 섞은 데이터가 +16%p 짜리 규칙을
  만들어 내는 판이라, 이 검정 없이 나온 이득은 읽을 수가 없다.
- **기권 규칙은 이득보다 `upShare` 를 먼저 본다.** 음수 예측만 버리는 규칙은 확신
  게이트가 아니라 상승장 편향이다. 이 저장소가 한 번 속았고, 메타 라벨링에서도
  같은 쏠림이 나왔다(20.2% → 27.8%).

## 검정은 `arch` 로 한다 — 손으로 만들지 말 것
- **덩어리 크기를 손으로 고르지 말 것.** `overfit.pick_block` 이 Politis & White 의
  최적 블록 길이로 정한다. 예전에 200 을 손으로 골랐는데 실제 계열은 **405판**씩
  뭉쳐 있었고, 그만큼 귀무 세계가 깨끗해져 문턱이 낮게 잡혔다(규칙 탐색 귀무 95%
  3.84 → 6.26%p). 결론은 안 바뀌었지만 `MIN_HOLDOUT_GAIN` 은 고쳐야 했다.
- **변이를 눈으로 세지 말 것.** `sweep.py --compare` 가 MCS(구별 안 되는 집합)와
  SPA(N개 중 최고가 기준선을 이겼나)로 답한다. 손으로 셀 때는 "시장요인은 암호화폐에서
  이기고 미국주식에서는 진다" 가 안 갈렸다.
- **손실을 견줄 때는 목표가 같은지 먼저 볼 것.** 잔차 학습은 시장을 뺀 알파를 맞히므로
  손실이 다른 값에 대한 것이다. 처음 MCS 를 돌렸을 때 그게 혼자 낮아서 "얘만 살아남았다"
  가 나왔다 — 잘 맞힌 게 아니라 쉬운 문제를 푼 것이다.
- **`arch` 는 `research` extra 다.** Actions 의 야간 작업 둘은 `.[ml]` 만 설치하므로
  `forecast/overfit.py` 는 **모듈 수준에서 `arch` 를 부르지 않는다** —
  `pick_block` 안에서만 부르고, `test_the_nightly_path_does_not_need_arch` 가 고정한다.
- 라이선스: `arch` 는 NCSA(퍼미시브). **GPL/AGPL 인 volatility-trading·openbb-technical·
  TTR 은 쓰지 않는다** — 공개 저장소에 옮는다. 범위 변동성은 논문 수식에서 직접 짰다.
- **지표 라이브러리는 안 들인다**(TA-Lib·ta). 지표 정답지가 두 벌이 되면 어긋났을 때
  무엇이 옳은지 모른다. 손계산 고정값과 불변성으로 잡는 지금 방식을 유지한다.

## 골라 뽑는 자리에는 문턱을 재서 건다
- **여러 번 시험하고 제일 좋은 걸 고르면 잡음만으로도 좋은 게 나온다.** 이 저장소는
  두 곳에서 그렇게 한다 — `daily.py` 의 챔피언·도전자, `study.py` 의 규칙 탐색.
  둘 다 시험 횟수를 로그에 적어 두고 **판정에는 안 썼던** 시절이 있었다.
- 문턱은 **감으로 정하지 말고 `scripts/overfitcheck.py` 로 잰다.** 결과를 섞어 놓고
  같은 탐색을 200번 돌린다. 거기서 나오는 이득은 전부 운이니 그 95%가 곧 문턱이다.
- **섞을 때는 덩어리째 섞을 것**(`BLOCK = 200`). 한 줄씩 섞으면 적중이 시간에 뭉쳐
  다니는 성질이 사라져 귀무 세계가 실제보다 깨끗해지고, 문턱이 너무 낮게 잡힌다.
  실제로 답이 뒤집혔다 — 한 줄씩 섞으면 200번 중 0개가 살아남아 "규칙은 진짜" 가
  되는데, 덩어리째 섞으면 194개가 살아남고 p 가 0.38 이다. 규칙이 헛것을 잡는 제일
  흔한 경로가 "좋았던 시기를 고른 것" 이라 그 구조를 지우면 재는 뜻이 없다.
- `daily.py` 의 승격 마진은 **고정값이 아니다.** `promote_bar(도전횟수, 잰 폭)` 이
  정하고, 폭은 로그의 도전자−챔피언 차이에서 잰다. 그날 쓴 문턱을 로그에 남긴다 —
  없으면 "왜 승격됐나" 를 재현 못 한다.
- 문턱은 시험 수의 **로그 정도로만** 자란다(100배 시험해도 두 배가 안 된다).
  잡음을 막자고 진짜 신호까지 막으면 안 되기 때문이다.
- 고쳤으면 `.venv/Scripts/python -m pytest server/tests/test_overfit.py -q` 와
  `.venv/Scripts/python scripts/daily.py --budget 1 --dry-run`.

## 종목 고르기와 검색

- **종목을 고르는 길은 하나다** — `App.selectSymbol(provider, symbol)`. 시장·종목·봉을
  한 핸들러 안에서 같이 바꾼다. 흩어 두면 어긋난다: 예전에 `draft` 상태가 따로 있어
  추천 목록에서 고른 종목이 헤더 입력창에 안 따라왔다. 그 상태를 없애 원인째 지웠다.
  세 `setState` 가 같은 이벤트 안에 있어야 React 가 한 번으로 묶고, 여기 매달린
  다섯 이펙트(실시간 재구독·분석·사건·상태 초기화·학습예측)도 한 번만 돈다 —
  사이에 `await` 를 끼우면 배치가 깨져 두 번 돈다.
- **새 시장이 지금 봉을 안 주면 일봉으로 간다.** `timeframes[0]` 으로 떨어뜨리면
  토스는 1분봉이 열려 사람이 아무것도 못 읽는다.
- **심볼을 대문자로 강제하지 말 것.** `005930.KS` 처럼 대소문자가 섞인 심볼이 망가진다.
  목록에서 고르면 정확한 심볼이 온다.
- 전체 목록은 `Provider.catalog()`, 검색은 그 기본 구현(`base.match`)이 목록을 거른다.
  **목록 코드를 두 벌 두지 말 것** — 그러면 목록 화면과 검색 결과가 서로 다른 종목을
  보여준다. 목록이 없는 시장(야후·us_stock·KIS)은 `catalog()` 가 빈 리스트고,
  그건 **오류가 아니라 '검색만 되는 시장'이라는 상태**다. 화면도 그렇게 표시한다.
- 항목 모양은 `base.item()` 한 곳에서 만든다(`symbol`·`label`·`name`·`market`·`kind`).
  프로바이더마다 라벨을 조립하면 한 화면에 섞였을 때 그게 그대로 보인다.
- **검색 순위의 부호는 `base.match` 한 곳**: 완전일치 → 앞부분 일치 → 부분 일치.
  동률은 **목록이 담아 온 순서**를 쓴다 — 거기에 시장의 우선순위가 들어 있다
  (바이낸스 USDT 먼저, 업비트 KRW 먼저, `base.prefer` 로 추적 종목 먼저).
  길이로 다시 줄 세우면 `BTC` 에 `BTCUSDT` 대신 `BTCTRY` 가 올라온다. 실제로 그랬다.
  구분자 뒤도 앞부분으로 친다 — 안 그러면 `KRW-BTC` 가 부분일치로 밀린다.
- **목록 캐시는 `SymbolCatalog` 하나.** TTL 12시간 · 동시 요청을 한 번으로 접기 ·
  실패하면 낡은 목록이라도 주기. 통합 검색이 모든 시장을 동시에 때리므로 접기가 없으면
  토스가 429 로 막힌다. **적재는 `shield` 로 감싼다** — 부르는 쪽이 시간 초과로
  취소돼도 적재는 끝까지 가야 한다. 안 그러면 토스처럼 첫 적재가 오래 걸리는 시장은
  몇 번을 쳐도 영원히 차갑다.
- 통합 검색은 **키가 있는 시장만** 친다(2.5초 상한). 빠진 시장은 `sources` 에 사유를
  남긴다 — 조용히 빠지면 "그 시장엔 그 종목이 없다"고 잘못 배우게 된다.
  같은 시장의 같은 심볼은 한 줄로 접고 나머지는 `also` 로 붙인다.
- **야후에 한글을 보내지 말 것.** 400 `Invalid Symbol` 로 거절한다. 보내기 전에
  걸러서 사람 말로 사유를 돌려준다.
- 화면은 4,300종목을 다 안 그린다. 앞 200줄 + **몇 개를 잘랐는지** 적는다.
  가상 스크롤 라이브러리는 넣지 않는다. 고른 시장 안 검색은 **로컬에서** 거르므로
  서버의 30건 상한을 안 탄다.
- ⚠ **헤더 종목 버튼 안에 `<b>` 를 쓰지 말 것.** `web/scripts/shot.mjs` 가
  `.topbar b` 로 시세 도착을 판정한다. 넣으면 스크린샷 전부가 빈 차트로 찍힌다.

## 화면은 폭 중심으로 짠다

27,664판을 채점한 결과가 이 배치의 근거다 — **80% 밴드 82.2%, 방향 55.0%.**
잘하는 것을 앞에, 못하는 것을 뒤에 둔다. 순서를 바꾸려면 숫자부터 다시 재야 한다.

- **카드가 셋 넘게 쌓이면 안쪽 탭으로 접는다**(`components/Tabs.tsx`의 `CardTabs`).
  오른쪽 띠는 320px 한 칸이라 나란히 놓을 수 없는 것이 전부 세로로 쌓인다. 추천 탭이
  국내주식·해외주식·코인을 다 펼치면 열여덟 줄이고, 셋째 묶음은 스크롤을 한참 내려야
  나왔다 — **그건 안 읽는다는 뜻이다.**
  - **이름은 다 보인다.** 셋을 한 번에 낸 이유는 "그런 게 있다는 걸 알게 하는 것"
    이었는데, 그 뜻은 이름이 늘 보이는 데 있지 목록이 펼쳐져 있는 데 있지 않다.
    **고르게 하지 않는다는 것과 한꺼번에 쏟아 놓는다는 것은 다르다.**
  - **빠진 칸도 자리를 지킨다.** 못 돈 시장은 흐리게 두되(`data-muted`) 지우지 않고,
    눌러서 사유를 볼 수 있게 `disabled` 도 아니다.
  - **비는 카드에는 칸을 만들지 않는다.** 누르면 아무것도 안 나오는 탭이 제일 나쁘다.
    그래서 카드가 스스로 받아 가던 자료는 화면이 한 번에 받아 둔다(`LearnPanel`).
  - 지금 접어 둔 곳: 추천(묶음 셋) · 예측(답 밑의 넷) · 학습(예측·자동 학습·성적표·
    기권) · 알림(알림함·규칙) · 근거(사건·등록부). 보유는 원래 같은 모양이었고
    같은 부품으로 갈아 끼웠다 — **모양을 따로 만들면 한 화면에 비슷하지만 다른 것이
    둘 생긴다.**
  - ⚠ **판단 탭은 접지 않는다.** 거기서는 예측(폭)과 종합 판단(방향)이 **위아래로
    같이 보이는 것 자체가 뜻**이다(밴드 82.2% · 방향 55.0%). 탭으로 나누면 방향만
    보고 나가는 길이 생긴다.
- **첫 탭은 추천**(변동 순위)이고 기본값이다. 예측·학습·판단이 그 뒤다.
- 모든 카드가 **폭을 맨 위에** 둔다: `ForecastCard` 는 `예상 변동 폭(80%)`,
  `LearnPanel` 의 학습 예측은 `n봉 뒤 변동 폭` 과 `밴드 실제 적중`.
- 방향은 **`방향 (참고)` 블록으로 내리고 `var(--text-dim)`** 을 입힌다. 지우지 않는다 —
  숨기는 것과 작게 두는 것은 다르고, 쓸지 말지는 보는 사람이 정한다.
- 추천 목록의 행에는 **변동 점수 하나만** 쓴다. 방향은 `<details>` 로 접는다.
  한 줄에 나란히 두면 같은 무게로 읽히는데, 실제로는 변동이 +0.94%p 를 가르고
  방향은 +0.03~0.65%p 다.
- **말이 먼저, 숫자는 접어 둔다.** 이 도구를 쓰는 사람이 분위수·적중률·skill 을
  공부했을 이유가 없다. `+0.17% · −3.7%~+4.3% · 오를 확률 52%` 만 내놓으면 읽는
  사람은 그게 좋다는 뜻인지 나쁘다는 뜻인지 알 수가 없다. **의사가 환자에게 말하듯**
  문장으로 먼저 말하고, 숫자는 `숫자 보기`(`components/Numbers.tsx`)로 접는다.
- **문장은 `web/src/say.ts` 한 곳에서 만든다.** 화면마다 적으면 같은 상황이 추천에서
  "거의 안 움직인다", 판단에서 "잠잠하다" 로 읽혀 두 화면이 다른 말을 하는 것처럼 보인다.
- **"크게" 만 적지 말고 얼마나인지 같이 적는다.** "크게 움직인다" 로는 3%인지 30%인지
  알 수 없다. 밴드 반폭(`대개 ±4.0%`)·확률(`오를 확률 52%`)·쏠림(`쏠림 78%, 문턱 24%`)을
  문장 안에 넣는다. **문장이 먼저고 숫자가 괄호 안**이라는 순서만 지키면 된다 —
  숫자가 앞에 오면 다시 숫자부터 읽게 된다.
- **"언제" 에도 답한다**(`say.whenWords` · `GET /api/recommend/symbol`). 추천과 판단이
  어긋나 보이는 제일 흔한 이유가 **지평**이다. 아침 추천은 1·2·3일을 **각각** 봤고 그
  답이 서로 다를 수 있다 — 실제로 삼성바이오로직스가 하루 뒤에는 `사라`, 이틀 뒤에는
  `사지 말 것` 이었다. 그걸 안 보여 주면 "추천은 사라는데 판단은 팔라네" 로만 읽힌다.
  ⚠ **`살 만한 셋` 에 든 날만 그렇게 부를 것.** 셋 다 아닌데 "그중 N일 뒤를 제일 좋게
  봤다" 고 쓰면 **없던 매수 신호가 생긴다.** 시점을 찍어 주지도 않는다 — 방향 적중이
  반반을 겨우 넘는 모델로 "언제 사라" 를 말하면 그 사실을 숨기는 화면이 된다.
- **통계 용어를 그대로 내보내지 말 것.** `t 값 2.01 유의` 는 t값을 모르면 못 읽고
  알면 굳이 문장으로 안 읽는다. 뜻을 풀어 쓰고(`우연으로 보기는 어려운 차이입니다`)
  숫자는 접어 둔다 — 지우면 확인할 길이 없어진다. `scenario/engine.py: summarize`
  가 내는 답 문장도 같은 규칙을 지킨다(서버가 만드는 유일한 사람 문장이다).
  ⚠ **사건 창이 겹치면 유의성을 아예 말하지 않는다.** 겹치면 사건들이 독립이 아니어서
  t 값이 실제보다 커지는데, 예전 화면은 `유의` 라고 띄우고 경고를 그 아래 따로 달았다 —
  읽는 순서상 "유의" 를 먼저 믿게 된다.
- **답을 되풀이하지 말 것.** 「답」 카드가 "그래서 어떻게 갔나" 를 말하면 「예측」 카드는
  **"그 답을 얼마나 믿을 만한가"** 만 맡는다. 둘 다 사례 수와 중앙값을 적으면 같은
  문장이 두 번 나오고, 그러면 둘 다 안 읽힌다.
- **숫자가 곧 답인 자리는 접지 말 것.** 보유의 손익(`지금까지 −₩3,487,500 (−69.75%)`)
  이나 실현손익은 말로 바꾸면 오히려 못 읽는다. 거기서 말이 필요한 건 **"그래서 지금
  어디쯤인가"** 쪽이라 손절선·목표까지 얼마나 남았는지를 문장으로 얹고, 진입가·평가
  손익 같은 **내역**만 접는다. 알림 기록도 변화율은 남기고 "그 뒤로 계속 올랐습니다"
  한 마디를 위에 붙인다 — 다만 **점수를 매기지 않는다**(`followup.py` 와 같은 이유).
- **숫자를 지우지는 않는다.** 접을 뿐이다. "믿지 마세요" 만 적고 근거를 감추면 읽는
  사람이 얼마나 안 믿어야 하는지 모른다 — 그건 더 나쁜 화면이다.
- **묶음 공통 이야기를 줄마다 되풀이하지 말 것.** 순위 차이·방향처럼 모든 행이 같은
  말을 하게 되는 것은 카드 위에 한 번만 쓴다. 행에는 그 종목만의 것(얼마나 움직일까)을
  남긴다 — 실제로 여섯 줄이 똑같아져 정작 다른 부분이 안 읽혔다.
- ⚠ **`관망 · 신뢰도 5%` 를 그대로 내보내지 말 것.** `signals/engine.py` 의 신뢰도는
  관망일 때 "관망이라는 판단"의 확신이 아니라 **쏠림의 크기**다. 5% 는 "관망을 5%만
  확신한다" 가 아니라 "규칙들이 거의 완전히 상쇄됐다" 는 뜻인데, 숫자로 내놓으면
  정반대로 읽힌다. 실제로 그렇게 읽혔다 — `say.verdictWords` 가 말로 옮긴다.

## 코인은 원화로 낸다
- **추천에 `SOLUSDT` 를 쓰지 않는다.** 원화로 사는 사람에게 달러 거래쌍 이름은 자기가
  치를 값과 무관하다. `솔라나 SOL` 처럼 이름과 티커를 같이 쓰고, 값은 원화로 적는다.
- **환율로 환산하지 말 것.** 바이낸스 달러 가격에 환율을 곱하면 숫자는 나오지만
  국내 시세는 김치 프리미엄만큼 따로 놀아서 **그 값에 살 수가 없다.** 투자 판단에 쓰는
  화면에서 못 사는 가격을 적는 건 틀린 것보다 나쁘다. `screen/coins.py: krw_market` 이
  업비트 원화 마켓으로 이어 주고, `scripts/recommend.py: krw_prices` 가 **추천이 딛고
  선 봉과 같은 날 종가**를 가져와 얼린다(지금 시세를 넣으면 같은 파일을 언제 열었느냐로
  값이 바뀐다).
- **원화 마켓이 없는 코인은 추천에서 뺀다**(`coins.NO_KRW` — 지금은 BNB·LTC).
  원화로 못 사는 것을 "오늘 살 만한 것" 에 올릴 수 없다. 지수를 빼는 것과 같은 이치라
  `universe.buyable()` 한 곳에서 같이 거른다.
  **학습에서는 그대로 쓴다** — `symbols()` 는 12종 그대로다. 표를 쪼개지 말 것.
- `NO_KRW` 는 손으로 적는다. 업비트 목록을 그때그때 조회하면, 얼리는 시각에 조회가
  실패한 날 추천 종목이 통째로 바뀐다. 조용히 달라지는 것보다 손으로 고치는 편이 낫다.
- **이름·티커는 얼린 파일에 넣지 않는다.** 표(`screen/names.py`)에서 바로 나오므로
  `api/recommend.py` 가 읽을 때 붙인다. 넣어 두면 표를 고쳐도 옛 파일이 옛 이름을
  계속 들고 나온다. 반대로 **원화 시세는 그날 값이라 반드시 얼린다.**
- 이름이 표에 없으면 티커만 낸다. **지어내지 말 것** — 다른 코인과 헷갈린다.
- 추천을 다시 얼릴 때(`--force`) **넘긴 시장만 갈아 끼운다.** 그대로 덮어쓰면
  `--force --provider binance` 한 번에 국내주식·미국주식이 그날 파일에서 사라진다.
- 고쳤으면 `.venv/Scripts/python -m pytest server/tests/test_coins.py -q`.

## 폰 앱 (PWA) 과 알림
- **iOS 는 홈 화면에 추가해야만 웹푸시가 온다**(16.4+). 사파리 탭으로 열어 두면 권한
  요청조차 안 뜬다. `AlertsPanel` 이 그 상태를 감지해 안내를 띄운다 — 지우지 말 것.
- **`dist` 뿌리의 파일을 SPA 폴백보다 먼저 준다**(`api/app.py: spa`). `/assets` 만
  마운트하면 `manifest.webmanifest`·`sw.js`·아이콘이 전부 `index.html` 로 나가고,
  서비스 워커가 HTML 을 받아 등록에 실패한다 — **그러면 푸시가 통째로 안 온다.**
  실제로 그렇게 만들었다가 잡았다.
- **서비스 워커는 `/api/*` 를 캐시하지 않는다.** 어제 가격을 오늘 값처럼 보여주면 안 된다.
  껍데기(HTML·JS)만 캐시한다.
- **알림 문구에 명령형을 쓰지 말 것.** "사세요" 가 아니라 "설정한 값에 닿았다" 다.
  방향 적중이 55% 인 모델로 명령을 하면 그 55% 를 숨기는 화면이 된다.
  `test_alerts.py` 가 금지어를 잡는다.
- **"언제 도달" 은 1·2·3일 지평으로만 쓴다.** 모델이 아는 시간 단위가 그것뿐이라
  "화요일 오후" 는 만들 수 없다. 지어내면 그 순간 거짓말하는 앱이 된다.
- **한 규칙은 한 번만 울린다**(`fired_at`). 경계에서 가격이 떨면 알림이 쏟아지고,
  그게 알림 앱을 못 쓰게 만드는 제일 흔한 이유다. 다시 받으려면 사람이 되살린다.
- 닿았는지는 **봉의 고가/저가**로 본다. 30초에 한 번 보는데 종가만 보면 찍고 돌아온
  값을 놓친다.
- **나간 알림을 지우지 않는다.** 화면에서 치우는 건 `archived` 표시다 — 알림이
  맞았는지가 이 기능의 유일한 성적표인데 기록을 지우면 영영 못 잰다.
- 폰 레이아웃에서 **가로 스크롤 컨테이너에 `min-width: 0`** 을 빠뜨리지 말 것.
  flex 자식은 기본이 `min-width: auto` 라 컨테이너가 내용만큼 늘어나고,
  스크롤 대신 **페이지가** 밀린다(실제로 35px 밀렸다).
  **격자 칸도 똑같다.** `.charts` 에 그게 없어서, lightweight-charts 가 캔버스에
  `width` 속성으로 박아 둔 고유 폭만큼 칸이 늘어나 390px 화면에서 페이지가 87px
  밀려 있었다. 차트 폭이 탭마다 달라 **보유 탭에서만** 나타났고, 그래서 한 탭만
  보고는 못 잡는다.
- **기준 기기는 아이폰 12(390×664, 사파리 크롬 제외).** 여기서 헤더가 세 줄로 접혀
  178px(화면의 27%)을 먹고 있었다. 헤더는 **한 줄**로 눕히고 넘치면 가로로 민다 —
  지금 33px 이고, 탭과 첫 카드가 첫 화면에 같이 보인다.
- 헤더에서 `order` 를 쓸 때 **`order` 를 안 준 형제는 0 이라 앞으로 튀어나온다.**
  `.topbar > * { order: 9 }` 로 기본을 뒤로 밀고 필요한 것만 1~4 를 준다. 그리고
  그 개별 규칙은 `.topbar > .price` 처럼 **`.topbar > *` 보다 무겁게** 써야 이긴다.
- **현재가는 봉 목록보다 앞에 둔다.** 헤더가 가로로 밀리는 구조라, 뒤에 두면 시세를
  보려고 헤더를 스크롤해야 한다. 거래 화면에서 그건 안 된다.
- 폰 확인은 `iPhone SE·12·12 Pro·12 Pro Max` **네 폭과 가로 모드**에서 한다 —
  가로 넘침 0, 현재가가 안 잘림, 탭이 첫 화면 안. 한 폭만 보면 어디로 틀어졌는지 모른다.
  **넘침은 숫자로 잰다**(`cd web; npm run phone`). 그림으로는 밀린 것과 원래 그런 것을
  구별할 수가 없어서 몇 달을 지나친다 — 실제로 87px 을 그렇게 지나쳤다. 탭마다 따로 잰다.
- 감시 테스트는 `daytime` 픽스처로 벽시계를 낮에 고정한다. 안 걸면 **새벽에 돌린
  사람만** 두 판이 빨개진다 — 23~07 시에는 급하지 않은 알림을 모아 두기 때문이다.

## 알림 기록

- **알림함과 기록은 다른 화면이다.** 알림함은 지금 봐야 할 것이고 기록은 그때 뭐가
  왔었나다. 한 목록에 두면 읽고 보관하는 순간 눈앞에서 사라지는데, 나중에 세어 볼 것이
  바로 그것들이다. 그래서 기록은 **보관한 것도 기본으로 보이고 지우는 버튼이 없다.**
- 목록은 `store.log()` 로 읽는다. `fired()` 는 알림함용이라 200 에서 끊으니
  **기록에 상한을 걸지 말 것** — 옛 알림이 조용히 사라진다.
- 뒷값(`alerts/followup.py`)은 **확정된 일봉만** 쓴다. 마지막 봉은 종가가 아직 안 굳어서,
  끼우면 제일 최근 기록만 조용히 틀린다. `+1일` 은 봉 하나 뒤라 주식은 거래일로 세어진다.
- 기준은 **닿은 값**(`entry["price"]`)이다. 설정값이 아니다 — 알림을 받은 사람이 본
  숫자가 그쪽이다.
- **점수를 매기지 말 것.** 나가는 것은 변화율까지다. `buy_below` 는 뒤가 오르면 반갑고
  `target_above` 는 이미 닿아서 나간 알림이라, 한 잣대로 더하면 없는 성적이 생긴다.
- 시세는 **종목당 한 번** 부른다. 같은 종목에 걸린 기록이 스무 건이면 스무 번이 되고,
  토스는 그 정도로 429 를 준다.
- 종류 이름표는 `AlertsPanel` 의 `KIND` 한 벌이다. 기록 화면이 그걸 가져다 쓴다 —
  두 벌이 되면 같은 알림이 화면마다 달리 읽힌다.
- 고쳤으면 `.venv/Scripts/python -m pytest server/tests/test_alert_log.py -q` 와
  `cd web; $env:SHOT_ONLY="log"; npm run shot` → `screenshots/chart-log.png` 를 Read 로 볼 것.
  `SHOT_ONLY` 는 한 장만 다시 찍는다 — 전부 찍으면 시세를 열네 번 받아 온다.

## 보유 — 산 것을 팔 때까지 따라간다

`positions/`. 알림이 "값이 닿았다"까지라면 여기는 "그래서 내 돈이 어떻게 됐나"다.

- **닿았다고 판 것으로 치지 않는다.** 이건 모의가 아니라 실제로 산 것을 적는 장부라,
  값이 닿아도 사람이 실제로 팔았는지는 프로그램이 모른다. 알림은 `hitAt` 을 적고 멈추고
  「팔았다」·「안 팔았다」는 사람이 누른다. 그 두 갈래가 곧 부분 익절과 트레일링 스탑이다.
  자동 체결로 만들면 장부가 실제 잔고와 갈라지고, 그 순간 손익은 아무 뜻도 없는 숫자가 된다.
- 손절·목표는 **80% 밴드에서** 나온다(`plan.py`). `-5% 손절` 같은 고정 비율을 쓰지 않는
  건 그 숫자가 이 도구가 잰 무엇과도 안 이어져서다 — 조용한 종목은 영영 안 닿고 시끄러운
  종목은 첫날 손절된다. 밴드가 없으면 ATR 로 내려가고, 그것도 없으면 **계획을 안 만든다.**
- **손절 규칙은 갈아 끼운다.** 올릴 때 옛 규칙을 지운다. 안 지우면 이미 올려 둔 자리
  아래에서 또 울린다. `test_positions.py` 가 규칙이 하나뿐인지로 잡는다.
- **통화를 더하지 않는다.** 원화 판과 달러 판은 성적표에서 따로 센다. 환율로 맞추지도
  않는다 — `screen/coins.py` 와 같은 이유다. 코인은 **원화 마켓(`KRW-SOL`)으로 연다.**
  추천은 바이낸스 모델로 나오지만 사람이 치른 값은 업비트 원화고, 밴드는 비율이라 그대로
  붙는다.
- 수익률은 **원금 대비**(`진입가 × 판 주수`)다. 남은 주수로 나누면 부분 익절한 판이
  두 배로 부푼다.
- `shares_left` 의 기본값은 **음수**다. 0 을 "안 적혔다"로 읽으면 다 판 판을 다시 읽을 때
  판 주식이 되살아난다. 실제로 그렇게 만들었다가 잡았다.
- **다음 할 일을 지어내지 말 것**(`advice.py`). 낼 수 있는 건 "근거가 어디서 끊겼고 다음
  근거가 언제 오나" 뿐이다. 손절은 밴드 아래끝을 뚫었다는 뜻이라 그 예측이 빗나간 것이고,
  목표까지 갔으면 다 쓴 것이다. 둘 다 답은 **다음 아침 추천까지**다. 오늘 추천에 그 종목이
  있으면 그 밴드 아래끝이 재진입 값이고, 없으면 없다고 한다.
- 탭이 아홉이라 `.tabs` 는 **줄을 바꾼다.** `flex: 1` 만 두면 320px 띠에서 칸이 34px 로
  눌려 두 글자 라벨이 세로로 쪼개진다("보/유").
- 추천 줄의 「샀다」 칸은 행의 **형제**다. 행 안에 넣으면 세 번째 flex 칸이 되어 종목
  이름을 짓누른다. 이름 칸에는 `min-width: 0` 이 있어야 줄어든다.
- 고쳤으면 `.venv/Scripts/python -m pytest server/tests/test_positions.py -q` 와
  `cd web; $env:SHOT_ONLY="hold"; npm run shot` → `screenshots/chart-hold.png`.

## 배포하지 않는다 — 이 PC 한 대로 돈다

한동안 Fly.io 배포가 준비돼 있었다(`fly.toml`·`Dockerfile`·`deploy.yml`). **지웠다.**
`FLY_API_TOKEN` 이 없어 실제로 한 번도 배포된 적이 없었고, 안 쓰기로 했기 때문이다.
되살릴 일이 생기면 git 에서 꺼내면 되지만, **반쯤 되살리지는 말 것** — 그 셋이 다
맞아야 화면이 기록을 읽는다(이미지가 `learning/` 을 담고, 배포 트리거가 `learning/**`
을 보고, 컨텍스트에서 무거운 것을 빼야 한다). 하나만 빠지면 화면이 "아직 없다"고만
말하고 왜 없는지는 어디에도 안 나온다. 실제로 그 상태였다.

- **토스는 이 PC 가 쓴다.** 클라이언트당 토큰이 하나라 두 곳이 같이 쓰면 서로 401 을
  주고받으며 양쪽 다 국내주식이 빈다. 지금은 쓰는 데가 여기뿐이라 문제가 없다
  (`scripts/recommend.cmd` 가 `toss_kr`·`toss_us` 를 넘긴다). Actions 는 IP 허용목록에
  막혀 못 돈다 — 그래서 국내주식 추천은 이 PC 에서만 나온다.
  (한동안 이 자리에 "그래서 `recommend.cmd` 에서 뺐다"고 적혀 있었는데 그 파일은
  만들어질 때부터 토스를 넘기고 있었다. **하려던 것을 한 것처럼 적으면 안 된다** —
  문서가 코드보다 오래 살아서, 읽는 사람이 확인 없이 믿는다.)
- **`MARKET_LENS_TOKEN` 은 안 켜도 된다.** 미설정이 기본이고, 그때는 잠그지 않는다.
  이 서버를 집 밖(공개 주소·LAN·터널)으로 내보내는 날이 오면 **그때 반드시 켠다** —
  없으면 라우트 스물다섯 개가 전부 열리고, 학습을 돌리는 `POST /api/train` 과 토스
  자격증명으로 부르는 국내주식이 같이 열린다.
- **폰 앱(PWA)과 웹푸시는 사실상 잠들었다.** iOS 웹푸시는 HTTPS 보안 컨텍스트와
  홈 화면 추가가 둘 다 필요한데, `localhost` 밖에서 그걸 만들 자리가 없어졌다.
  **코드는 지우지 않았다** — 감시 루프(`MARKET_LENS_WATCH=1`)는 이 PC 에서 그대로
  돌고 알림함·기록은 화면에서 읽힌다. 폰으로 받고 싶어지면 배포를 되살리는 게
  제일 짧은 길이다.

## 종목 이름은 한글로, 표는 한 벌
- `AAPL`·`005930`·`SOLUSDT` 는 사람이 읽는 이름이 아니다. 추천은 이 도구에서 제일
  자주 읽는 화면이라 티커를 외우고 있어야 읽히면 안 된다. `screen/names.py` 한 곳에서
  이름을 붙이고, 화면은 `삼성전자 005930` 처럼 이름과 기호를 같이 쓴다.
- **이름을 지어내지 말 것.** 토스가 주는 이름을 그대로 옮겨 적었다
  (`/api/symbols?provider=toss_kr|toss_us`, 2026-08). "제이피모건 체이스"·"알파벳 A"
  처럼 어색해 보여도 **거래 화면에서 쓰는 그 이름**이라 다듬지 않는다 — 다듬으면
  사용자가 토스에서 본 이름과 달라진다.
- **실행 중에 조회하지 않는다.** Actions 는 토스에 못 닿고(미국주식 추천에 이름이
  안 붙는다), 추천을 읽는 API 는 읽기 전용이며, 조회가 실패한 날 이름이 통째로
  사라진다. 유니버스는 스무 종목 남짓이라 표가 훨씬 싸다.
- **유니버스를 늘리면 표도 같이 늘린다.** `test_names.py` 가 "추천에 오를 수 있는
  종목은 전부 이름이 있다"를 잡는다 — 없으면 목록에 티커만 덩그러니 뜨는데,
  그건 화면을 열어 봐야 알게 되는 종류다.
- **주식 표를 코인보다 먼저 본다.** 코인은 거래쌍을 떼는데(`SOLUSDT` → `SOL`),
  그 규칙을 주식에 먼저 대면 `XXXETH` 같은 심볼이 갉인다.

## 판단은 어느 봉을 보고 한 말인지 밝힌다
- **같은 종목도 봉이 다르면 판단이 다르다.** SOLUSDT 가 같은 날 1시간봉에서 매도
  (신뢰 0.26), 4시간봉·일봉에서 매수(0.81 · 0.92)였다. 둘 다 맞고 질문이 다른 것이다.
- 아침 추천은 **일봉**으로 1~3일을 본 결과다(`RECOMMEND_TIMEFRAME`). 그런데 화면의
  기본 봉은 1시간이라, 추천에서 종목을 눌러 들어가면 "추천은 사라는데 판단은 팔라네" 가
  된다. 실제로 그렇게 보였다.
- 그래서 둘을 건다: **추천에서 열면 그 봉으로 맞추고**(`selectSymbol(…, wanted)`),
  **종합 판단 제목 옆에 선 봉을 적는다**(`1d 기준`). 봉이 추천과 다르면 그때만
  안내문이 붙는다 — 오류가 아니라 안내라서 경고색을 쓰지 않는다.
- `RECOMMEND_TIMEFRAME` 은 `scripts/recommend.py` 의 모델 이름(`…-1d-<일수>`)과 짝이다.
  한쪽만 바꾸면 화면이 조용히 거짓말을 한다.
- **성적을 누구 것인지 밝힐 것.** "이 추천이 과거에 맞았나" 칸 아래의 방향 55.0% ·
  밴드 82.0% 는 **추천의 성적이 아니라 밑에 깔린 모델의 성적**이다. 추천은 오늘
  시작해서 `record.n` 이 0 이다. 붙여 놓으면 추천의 실적으로 읽힌다.

## 스크롤바
- 기본 스크롤바를 쓰지 않는다. 윈도우 것은 폭 17px 에 흰 트랙이라 320px 짜리 오른쪽
  띠에서 5% 를 먹으면서 혼자 밝다.
- **`*` 에 건다.** 스크롤 나는 자리를 세어 두면(지금 다섯 곳) 새 목록을 만들 때 거기만
  기본 스크롤바가 남는데, 그건 눈으로 잘 안 잡힌다.
- **표준 속성과 의사요소를 같이 걸지 말 것.** 크롬 121 부터 `scrollbar-width` 를
  지원하는데, 그게 붙어 있으면 `::-webkit-scrollbar` 를 **통째로 무시한다.** 실제로
  그렇게 썼다가 막대에 아무 모양도 안 잡혔다. 파이어폭스만
  `@supports not selector(::-webkit-scrollbar)` 로 감싸 표준 속성을 준다.
- 트랙은 칠하지 않는다. 카드 배경이 저마다 달라(`--panel`·`--panel-2`·오버레이)
  한 색으로 칠하면 어딘가에서 띠가 도드라진다.
- 막대의 여백은 `border: 2px solid transparent` + `background-clip: padding-box` 로
  만든다. 폭을 줄이면 **잡는 면적까지 줄어드는데** 이 방법은 10px 을 유지한다.
- `:root { color-scheme: dark }` 를 지우지 말 것. 스타일이 안 먹는 자리(입력창 화살표,
  기본 폴백)에서 흰 스크롤바가 비친다.
- **헤드리스로는 확인이 안 된다.** `npm run shot` 의 크롬은 오버레이 스크롤바라
  스크린샷에 안 그려지고 레이아웃 폭도 0 이다. 규칙이 먹었는지는
  `document.styleSheets` 를 뒤져 확인하고, 모양은 실제 브라우저에서 볼 것.

## 패널 시간축은 맞물려 있어야 한다

차트를 확대하면 RSI·MACD 도 같이 확대돼야 한다. 안 그러면 같은 26일을 같은 선상에서
못 본다. lightweight-charts **4.2** 라 v5 의 단일 차트 다중 pane 은 못 쓴다 —
`IChartApi` 를 여러 개 만들어 손으로 맞추는 게 유일한 길이고, 그러려면 셋이 다 맞아야 한다.

- **값이 없는 자리를 버리지 말 것.** `core/series.py: _points` 가 warm-up·결측을
  **whitespace**(`{time}` 만)로 채운다. 버리면 RSI(14) 의 첫 점이 캔들 14번째가 되고,
  logical 인덱스는 "그 차트의 첫 데이터" 기준이라 메인의 0번과 보조의 0번이 14봉
  (MACD 는 34봉) 어긋난다. 같은 범위를 넘겨도 다른 구간이 보인다.
  값이 하나도 없으면 **빈 배열** — 화면이 그걸 보고 시리즈를 아예 안 만든다.
- **재진입 가드는 프레임을 넘겨야 한다.** v4 의 `setVisibleLogicalRange` 는 그 자리에서
  적용되지 않고 다음 페인트에 적용되며 거기서 이벤트가 난다. 같은 틱에서 켰다 끄는
  플래그로는 되쏘기를 못 막아 핑퐁이 된다. **마지막으로 넘긴 범위를 차트마다 기억해
  두고 같은 범위면 무시**한다.
- **구독을 매 틱 다시 걸지 말 것.** deps 는 `paneKey`(패널 id 를 이은 문자열)다.
  배열을 그대로 쓰면 실시간 틱마다 재구독하면서 범위를 강제로 맞춰, 방금 스크롤한
  자리가 되돌아간다.
- 서브 패널에도 **`ResizeObserver`**. `window.resize` 만으로는 탭 전환·패널 추가처럼
  레이아웃만 변할 때를 못 잡아 서브만 옛 폭을 유지한다.
- `rightPriceScale.minimumWidth` 로 패널들의 가격축 폭을 맞춘다. 폭이 다르면 같은
  범위라도 세로선이 어긋난다.
- 크로스헤어도 같이 움직인다(`subscribeCrosshairMove` → `setCrosshairPosition`).
  이건 **시각 기준**이라 인덱스가 어긋나도 맞는다 — 그래서 이것만 맞는다고
  다 된 줄 알면 안 된다.
- 고쳤으면 확대한 상태로 캡처해 **세 패널의 가로축 눈금이 같은 날짜인지** 눈으로 볼 것.
  `screenshots/chart-zoom.png`.

## 화면에서 지킬 것

- **예측선은 세로축 계산에 끼지 않는다**(`autoscaleInfoProvider: () => null`).
  끼면 사례 하나가 +20% 로 뻗은 순간 축이 늘어나 정작 봐야 할 캔들이 아래에 눌린다.
- 같은 시각의 마커는 하나로 합친다. lightweight-charts 는 겹친 마커를 하나만 그리고
  나머지를 조용히 버린다.
- 사건 조회 구간은 차트가 보여주는 구간과 같게. 더 넓게 받으면 "사건 300건" 이라 해놓고
  화면에는 세 개만 찍힌다.
- 실시간이 원래 없는 프로바이더(야후)는 **오류가 아니라 상태**다. 빨간불로 깜빡이면 안 되고,
  3초마다 재시도해서도 안 된다.

## 검증

**문서에 `&&` 를 적지 말 것.** 이 프로젝트는 Windows PowerShell 5.1 에서 돌고,
거기서는 `&&` 가 문 구분 기호가 아니라 `올바른 문 구분 기호가 아닙니다` 로 죽는다.
이어 쓸 때는 `;` 를 쓴다. 실제로 README 의 `cd web && npm install` 이 그대로 붙여넣기
안 되는 명령이었다.

```bash
.venv/Scripts/python -m pytest server/tests -q      # 549개
.venv/Scripts/python scripts/daily.py --budget 2 --dry-run   # 승격 없이 한 바퀴
.venv/Scripts/python scripts/backfill.py --summary            # 되돌려 본 추천 성적
.venv/Scripts/python scripts/backfill.py --test               # 그 차이가 잡음과 갈리는지
.venv/Scripts/python scripts/screen.py --dry-run             # 추천 팩터 측정
cd web; npx tsc -b
cd web; npm run shot     # uvicorn(8000) + vite(5173) 가 떠 있어야 한다
cd web; npm run phone    # 네 폭 + 가로 모드에서 가로 넘침이 0 인지
```

`npm run shot` 은 실제로 질문을 던져 답이 뜰 때까지 기다린다. `chart-predict.png` 에서
예측 경로가 마지막 봉 **다음**부터 그려지는지, `chart-ichimoku.png` 에서 구름이 26봉
앞까지 뻗는지는 그림으로만 확인된다.

지표의 정답지를 다른 라이브러리에서 빌려오지 않는다 — 버전마다 값이 달라 무엇이 옳은지
모르게 된다. 손계산 고정값과, 정의상 참이어야 하는 성질(RSI∈[0,100], 볼린저 상단≥중단≥하단,
선행스팬 = 26봉 시프트)로 잡는다.

## git 규칙

- 한 단계 작업이 끝나면 main 에 병합하고 push 한다. 브랜치에 오래 쌓아두지 않는다.
- push 는 사용자가 요청할 때만. 단 작업 완료 보고 시 "push 필요 여부"를 항상 알린다.
- force push 와 rebase 는 하지 않는다.
