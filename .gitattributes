# 배치 파일은 어디서 클론하든 CRLF 여야 한다. LF 면 cmd.exe 가 줄을 못 끊어
# 한글 REM 주석을 명령으로 읽고, 작업 스케줄러는 그걸 숫자 1 하나로만 알려 준다.
*.cmd text eol=crlf
*.bat text eol=crlf

# 반대로 셸 스크립트는 LF 여야 한다. CRLF 면 Actions 의 우분투에서 안 돈다.
*.sh text eol=lf
