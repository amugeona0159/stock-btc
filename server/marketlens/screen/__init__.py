"""종목 추천 — 오늘 관심있게 볼 종목을 횡단면으로 줄 세운다.

세 조각이다:

- `universe` — 후보 종목 표(한 벌). 학습 동료 목록도 여기서 나온다.
- `factors`  — 줄 세울 축. 새로 만들지 않고 학습 표의 인과적 축에서 고른다.
- `ic`       — 그 축이 실제로 줄을 세우는지 재는 곳. 부호도 여기서 정해진다.
- `rank`     — 잰 결과로 오늘의 순위를 만든다.

**재지 않은 지평은 순위를 안 만든다.** 그럴듯한 목록보다 빈 화면이 낫다.
"""
from . import factors, ic, rank, universe  # noqa: F401
